spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:emlakjet_db
    driver-class-name: org.h2.Driver
    username: sa
  h2:
    console:
      enabled: true

purchasing-config:
  limit:
    max: 200